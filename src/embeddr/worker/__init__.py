"""
Worker package for headless embeddr instances that connect to a main coordinator.

A worker node runs plugins, processes jobs, and streams results back to the
main instance — but does not serve the UI or manage auth directly.
"""
